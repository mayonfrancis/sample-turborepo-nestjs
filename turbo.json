{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "tasks": {
    "start:dev": {
      "inputs": ["$TURBO_DEFAULT$", ".env*", "dist"],
      "cache": true,
      "persistent": true
    }
  }
}
